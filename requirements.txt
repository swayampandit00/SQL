# COMMAND-BASED PYTHON SQL EXECUTOR - Requirements
# Professional Database Management Tool

# Core Python Libraries (built-in)
# No external packages required for basic SQLite functionality

# Optional Database Connectors
# Install these based on your database needs:

# MySQL Support
mysql-connector-python>=8.0.0

# PostgreSQL Support  
psycopg2-binary>=2.9.0

# Optional Enhanced Features
# Uncomment and install if needed:

# For enhanced CSV export with better formatting
tabulate>=0.9.0

# For colored terminal output (optional)
colorama>=0.4.0

# For better password input handling (enhanced security)
#getpass>=0.1.0

# For advanced table formatting
prettytable>=3.0.0

# Installation Instructions:
# 1. Basic SQLite (no additional packages needed):
#    pip install -r requirements.txt
#
# 2. Full MySQL support:
#    pip install mysql-connector-python
#
# 3. Full PostgreSQL support:
#    pip install psycopg2-binary
#
# 4. All features:
#    pip install -r requirements.txt
#    pip install mysql-connector-python psycopg2-binary

# Python Version Requirement
python>=3.6

# Notes:
# - SQLite is built into Python, no additional installation required
# - MySQL and PostgreSQL connectors are optional based on your database needs
# - All security features use built-in Python libraries (hashlib, getpass)
# - Application will work with just Python 3.6+ for SQLite databases
